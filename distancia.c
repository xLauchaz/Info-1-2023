#include <stdio.h>
int main(void)
{
    float v;
    float t;
    float m;
    printf("Ingrese la velocidad de su movil: ");
    scanf("%f", &v);
    printf("Ingrese el tiempo que recorrio el movil: ");
    scanf("%f", &t);
    printf("la distacia recorrida por el movil es de: %.3f metros\n",m=v*t);
    return 0;
}
